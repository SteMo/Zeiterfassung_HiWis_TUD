Ext.define('AM.controller.Aufgaben', {
    extend: 'Ext.app.Controller',

    stores: [
         
    ],
	
    views: [
        
    ],
	
	models: [
		
	],
	

    init: function() {
    },


    // editUser: function(grid, record) {
        // var view = Ext.widget('useredit');

        // view.down('form').loadRecord(record);
    // }
});